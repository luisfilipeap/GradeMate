interface:
  display_name: "Review Task"
  short_description: "Delegate a task review to a named agent"
  default_prompt: "Use $review-task TASK-001 reviewer to delegate an independent task review."

policy:
  allow_implicit_invocation: false
