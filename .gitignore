# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Environment
.env
.env.local

# Editors / OS
.idea/
.vscode/
.DS_Store

# Local data
storage/
*.log

# Real exams used while developing. They contain students' personal data and
# must never be committed.
data-test/

# ============================================================================
# MyDevOffice Agent Governance Framework — LOCAL ONLY
# ============================================================================
# These files are PART OF THE GRADEMATE WORKING ENVIRONMENT, NOT part of the
# production codebase. They enable agent-to-agent communication and local
# skill execution. They must NEVER be committed to the repository.
#
# **IMPORTANT FOR ALL AGENTS:**
# - DO NOT commit these files
# - DO NOT delete these files (they enable agent communication)
# - DO keep them updated locally as work progresses

# Agent role definitions and skill implementations (MyDevOffice framework)
.claude/agents/
.codex/agents/

# Agent governance policies
AGENTS.md
PERMISSIONS.yml

# Architecture decision records (local planning, not production docs)
docs/architecture/decisions/

# Local agent-specific configuration (never versioned)
.codex/config.toml

# Agent-to-agent communication artifacts (local only — DO NOT DELETE)
# These files persist locally and enable agent coordination
.ai/recommendations/
.ai/reviews/
.ai/
